.row
  %br
  .span12.inline
    %a.logo{:href => '/'} Google Play With Me

.row
  .span12
    .page-header
      %h3 #{link_to @app_id, "/apps/#{@app_id}"}/<wbr />#{@path}

- coderay = CodeRay.scan(@results.results.first.lines.join("\n"), @extension)
~ raw coderay.html :line_numbers    => :table,
                   :highlight_lines => [params[:hl].to_i]

%hr
